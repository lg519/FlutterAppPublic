name: flutter_application_1
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.36.0
  charts_flutter: ^0.12.0
  english_words: ^4.0.0
  provider: ^6.0.0
  syncfusion_flutter_charts: ^21.2.4
  terra_flutter_rt: ^0.1.1
  http: ^0.13.6
  tflite_flutter: 0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/baseline.tflite
    - assets/LSTM.tflite
    - assets/LSTM_autoregressive.tflite
